One-line: the hero's gradient panel with the breathing glow — one per page, in the hero only.

\`\`\`jsx
<GlowOrb size={420}><img src="assets/hero.png" alt="" style={{width:'100%'}} /></GlowOrb>
\`\`\`

Never place two on a page and never reuse the pulse animation elsewhere; it is the signature move.
