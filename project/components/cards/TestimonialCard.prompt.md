One-line: alumni quote card; the only card type that lives on a pale tint rather than Elevated.

\`\`\`jsx
<TestimonialCard tint="teal" quote="CodeKerdos didn't just teach me skills - it transformed the way I think and solve problems."
  name="Vrutik Patel" outcome="Microsoft" />
\`\`\`

Rotate \`tint\` across a grid so no two neighbours match. Quotes stay 2-3 lines; the outcome line carries the company.
