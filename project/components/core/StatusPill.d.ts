/** Solid-green status pill with a dot — used for the "LIVE" flag on live-class features. */
export interface StatusPillProps { children?: React.ReactNode }
export function StatusPill(props: StatusPillProps): JSX.Element;
