/** Uppercase +1.6px-tracked label that sits above every section heading. */
export interface EyebrowProps {
  tone?: 'cyan' | 'muted' | 'ink';
  children?: React.ReactNode;
}
export function Eyebrow(props: EyebrowProps): JSX.Element;
