One-line: the page's final ask — one headline, one button, two proof lines.

\`\`\`jsx
<CtaBand eyebrow="Next cohort" title="You're one call away from" emphasis="your next role."
  action={<Button size="lg">Book A Free Webinar</Button>}
  benefits={['5-6 months, not 8-10', '15+ deployable projects']} />
\`\`\`

One per page, immediately before the footer. This is the second and last place the glow pulse appears.
